# Database - PostgreSQL connection string (Neon, Vercel Postgres, or local)
DATABASE_URI=postgresql://user:password@localhost:5432/gst_site

# Payload CMS secret - used for encryption (generate a strong random string)
PAYLOAD_SECRET=CHANGE-ME-generate-a-random-secret

# Public site URL
NEXT_PUBLIC_SITE_URL=http://localhost:3000
